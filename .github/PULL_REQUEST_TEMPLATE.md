## What changed

<!-- One or two sentences. The diff shows what; say what it does. -->

## Why

<!-- The problem, or the finding this closes. If it is a bug, what went wrong. -->

## How it was verified

<!--
Name the commands you ran and what they printed. "Tests pass" is not
verification; `npm run check` -> 584 passed / 2 skipped is.

If this fixes a bug, say how you saw it fail before the fix.
-->

- [ ] `npm run check`
- [ ] `npm run build`
- [ ] `npm run format:check`
- [ ] `npm run docs:lint`
- [ ] `npm run docs:api`
- [ ] `npm run test:e2e` (if this touches the UI or the routes)

## Closes

<!-- Issue number, or an audit finding ID. Delete if neither applies. -->
