# API Configuration
# Replace these values with your actual AWS resource URLs

# Base URL for API Gateway endpoints (Lambda functions)
# Example: https://abc123xyz.execute-api.us-east-1.amazonaws.com/prod
VITE_API_BASE_URL=https://your-api-gateway-url.amazonaws.com/prod

# CloudFront CDN URL for static content (poems, authors)
# This serves the daily poem JSON files and author data
VITE_CDN_BASE_URL=https://your-cloudfront-distribution.cloudfront.net

# S3 bucket name for storing JSON data
VITE_S3_BUCKET=your-bucket-name

# AWS Region where resources are deployed
VITE_AWS_REGION=us-east-1
