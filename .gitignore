# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
build/
dist/
frontend/build/
frontend/dist/

# Generated by scripts/split-poets-json.js — ~1,600 JSON files staged for S3
output/

# Test output
coverage/
test-results/
playwright-report/
playwright/.cache/

# lychee-action writes its summary here. Ignored so it can never be picked up by
# the markdownlint run in the same job (which reads .gitignore) or by Prettier.
lychee/

# typedoc output (`npm run docs:api`). Deliberately NOT committed: generated HTML
# has to be regenerated on every source change and is stale within a week, which
# is the drift the reference exists to remove. CI runs the generation instead, so
# a malformed doc comment fails the build. Publishing a hosted copy is a separate
# decision and the repository owner's.
docs/api/

# Environment files (contain secrets)
.env
.env.local
.env.*.local
frontend/.env
backend/.env.deploy

# SAM build artifacts
.aws-sam/
backend/.aws-sam/

# IDE
.vscode/
.idea/

# OS
.DS_Store

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Misc
*.sample
.secret-*

# Generated hook shims. lefthook.yml is the hook source of truth; `lefthook
# install` (via postinstall) provisions .git/hooks/, which git never tracks.
# A .husky/ directory here would be a generated shim embedding an absolute
# developer path -- exactly what was removed in the 2026-08-09 remediation.
.husky/

# One lockfile governs the workspace; a frontend lockfile drifts from it
frontend/package-lock.json

# Agent tooling state
.claude/

# 22 MB source dataset for scripts/split-poets-json.js — not consumed by the app.
# Untracked, but still present in git history, so clone size is unchanged by this
# entry. Removing it from history would take a filter-repo rewrite and a
# force-push, invalidating every existing clone, fork and open PR -- an
# outward-facing action deliberately left to the repository owner.
/poets.json

# Legacy (can be removed after cleanup)
/public/holder
/public/json
/archive
/scrape_data
awsconfiguration.json
amplifyconfiguration.json
aws-exports.js
todolist
scp.txt
cleaning.py
moving.py
.babelrc
